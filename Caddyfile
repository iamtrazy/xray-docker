{
	order reverse_proxy before route
	admin off
	log {
		output file /usr/share/caddy/caddy_access.log
		level INFO
	}
}

:8080 {

	root * /usr/share/caddy

	file_server

	@vws {
		path /iamtrazy
		header Connection *Upgrade*
		header Upgrade websocket
	}
	reverse_proxy @vws http://localhost:8081
}
